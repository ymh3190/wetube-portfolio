extends layouts/main
include mixins/videoSearching

block content
    each item in videos
        +videoSearching({
            videoFile:item.fileUrl,
            id:item.id,
            title:item.title,
            views:item.views,
            createdAt:item.createdAt,
            description:item.description
        })