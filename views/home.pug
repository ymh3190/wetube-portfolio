extends layouts/main
include mixins/videoContent

block content
    .videos
        each item in videos
            +videoContent({
                videoFile:item.fileUrl,
                id:item.id,
                title:item.title,
                views:item.views,
                createdAt:item.createdAt,
                description:item.description
            })